#include <stdio.h>
#include <math.h>

int main() {
    int x, y,result;

    printf("Enter x value: ");
    scanf("%d", &x);

    printf("Enter y value: ");
    scanf("%d", &y);

    result = pow(x, y);

    printf("Your Answer: %.d", result);

    return 0;
}
